echo "this is for all Env"
stages {
        stage('checkout') {
            steps {
git branch: 'main', 
  credentialsId: 'git-creds',
  url: 'https://github.com/suresha10/newclass.git'
            }
        }
